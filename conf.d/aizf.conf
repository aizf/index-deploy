server {
    listen 80;
    # listen [::]:80;
    server_name aizf.ink;
    return 301 https://$server_name$request_uri;
}

server {
    listen 80;
    listen 443 ssl;
    server_name www.aizf.ink;

    ssl_certificate     /etc/letsencrypt/live/aizf.ink/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aizf.ink/privkey.pem;

    if ($server_name ~ ^www.(.*)){
        return 301 https://$1$request_uri;
    }
}

server {
    listen      443 ssl;
    server_name aizf.ink;

    location / {
        root /var/www/index/;
        index  index.html index.htm;
    }

    location /projects/ {
        alias /var/www/projects/;
        index  index.html index.htm;
    }

    # add_header X-Content-Type-Options nosniff;

    ssl_certificate     /etc/letsencrypt/live/aizf.ink/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aizf.ink/privkey.pem;
}